regarde j'ai affiché linux_généric...

en même temps y'a rien d'extraordinaire mais j'ai un peu avancé...
D'extroardinaire peut-être pas, mais de constructif ça oui; parce que c'est la base de l'IHM ça quand même.

Il ne reste plus qu'à mettre une couleur (ou un symbole, une image, etc) quand le status est OK, UNKNOWN et DOWN. Et l'interface web minimale sera réalisée :D 

Après il faudra ajouter un peu d'interactivité si le temps le permet:
- ack (acknowledgment) -> Pour valider que l'admin à bien vu qu'il y avait un problème
- un clic sur un périphérique ouvre  une page avec les diférents services surveillés et leurs états
- un clic sur un service ouvre une page avec un historique des états de ce service

 Et si il reste encore du temps :
- des jolis graphiques pour visualiser ces historiques d'état.
-> "cacti" ou "munin" devraient largement faire l'affaire.
...
 ntop et son interface web -> à intégrer dans le superviseur ^^
et nmap (zend) ausi
